{
  "name": "hsit-backend-root",
  "version": "1.0.0",
  "description": "Backend for HSIT app",
  "type": "module",
  "main": "backend/server.js",
  "scripts": {
    "start": "node backend/server.js",
    "build": "npm install --prefix frontend && npm run build --prefix frontend",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "dependencies": {
    "bcryptjs": "^2.4.3",           // <-- Added bcryptjs
    "cors": "^2.8.5",
    "dotenv": "^16.4.5",
    "express": "^4.19.2",
    "express-validator": "^7.1.0", // <-- Added express-validator
    "jsonwebtoken": "^9.0.2",       // <-- Added jsonwebtoken
    "mongoose": "^8.4.0"
  },
  "engines": {
    "node": ">=18.0.0"             // Recommended: Specify a stable Node version
  },
  "author": "",
  "license": "ISC"
}
