// backend/routes/users.js
import bcrypt from 'bcryptjs'; // <-- Node.js cannot find this package
// ... rest of the file

{
  "name": "hsit-backend-root",
  "version": "1.0.0",
  "description": "Backend for HSIT app",
  "type": "module",
  "main": "backend/server.js",
  "scripts": {
    "start": "node backend/server.js",
    "build": "npm install --prefix frontend && npm run build --prefix frontend",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "dotenv": "^16.4.5",
    "express": "^4.19.2",
    "mongoose": "^8.4.0"
  },
  "engines": {
    "node": ">=18.0.0"
  },
  "author": "",
  "license": "ISC"
}
